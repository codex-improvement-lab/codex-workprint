# Codex Workprint 0.1.0-rc.1 historical release-candidate note

This file preserves the rc.1 handoff as it stood on 2026-08-29. Its evidence remains under the dated `docs/evidence/**/2026-08-29` files. Current live demo bytes have since advanced to rc.2; do not treat rc.1 hashes as bindings for the current demo.

## Surface

rc.1 introduced the versioned Codex JSONL adapter, Workprint IR v0.1, deterministic seven-file HTML/SVG/PNG bundle, default-deny privacy projection, `inspect`/`build`/`verify`, Windows/macOS Node 22/24 CI configuration, and a fail-closed physical-macOS handoff.

## Observed evidence at that handoff

- Windows bundled Node v24.19.0: 16/16 tests and 89/89 release checks.
- Two fresh, no-secret Codex CLI 0.145.0 inputs compiled into different public Workprints.
- Real browser desktop and narrow checks on Windows with zero console warnings/errors and honestly named JPEG screenshots.
- External CI configured but not observed; physical macOS, real users/market, public identity, commit/tag/remote/package publication all pending.

The dated receipts, not this summary, are the evidence authority. None of the above established source authenticity, task correctness, physical-macOS behavior, author identity, or user/market outcomes.
