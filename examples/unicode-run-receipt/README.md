# Unicode Run Receipt example

This is a deterministic Workprint generated from the repository's synthetic failure-followup fixture with the explicit public title `修复登录失败并验证发布`, project `Workprint 示例`, release `0.3.0-rc.1`, and language `zh-Hans`.

It is a renderer/readability example, not a real-user, source-authenticity, task-correctness, or internationalization-completeness claim. The pinned bitmap source covers Unicode Plane 0; complex-script shaping and supplementary-plane coverage are outside this candidate's claim.

Refresh and verify from the repository root:

```text
node ./scripts/build-unicode-example.mjs --refresh
node ./scripts/build-unicode-example.mjs
```
